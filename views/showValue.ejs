<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Display Price</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="mt-4">Mail Pricing</h1>
        <div class="mt-3"><strong>
            <% if (cost == undefined) { %>
                <p>Sorry, <%=weight.toFixed(1)%> is an invalid weight for shipping a <%=type%> letter/parcel</p>
            <% } else { %>
                <h2>$<%= cost.toFixed(2) %></h2>
                <p>Your letter/package weighing <%= weight.toFixed(1) %> oz will cost $<%= cost.toFixed(2) %> to ship as a <%=type%> letter/parcel.</p>
            <%}%>
            <button class="btn btn-primary" onclick="window.location.href = 'calculator.html';">Back to Calculator</button>
        </strong></div>
    </div>
</body>
</html>
